import React from "react";

import React from "react";
// import "../Styles/Navbar/AnimationMenu.css";
const Modal = ({ isOpen, onClose, children }) => {
  if (!isOpen) return null; // Si el modal no está abierto, no se renderiza

  return (
    <div className="cajabonita">
        <h1>HORARIOS</h1>
        <input type="text" Cursos />
        <input type="text" Materias/>
        <button>Generar Horarios</button>
        <button>bkdsed</button>
        <div>Tabla de horarios </div>
        <a href=""></a>
        <img src="" alt="" />

        <h1>titulo</h1>
    </div>
   
  );
};

export default Modal;
